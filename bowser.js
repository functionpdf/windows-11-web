//usage with bowser lib

var browserInfo = {
    name: bowser.name,
    version: bowser.version
};

console.log("Browser Name: " + browserInfo.name);
console.log("Browser Version: " + browserInfo.version);
